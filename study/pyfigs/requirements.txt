# Lo que hace falta para regenerar las figuras del articulo.
# Versiones minimas, no fijadas: nada de lo que se usa ha cambiado de API
# en estas ramas. Probado con numpy 2.4.2, scipy 1.17.0, matplotlib 3.10.8.
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
